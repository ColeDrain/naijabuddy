# NaijaBuddy — Numbers Integrity

Every quantitative claim in `solution_paper.md` Section 4, mapped to the exact
command that regenerates it. All evaluation is leakage-free leave-one-out; the
LLM artifact cache (`eval_artifacts/llm_cache.jsonl`) makes re-runs deterministic
and free of new GPU cost. Domains are always listed Yelp / Goodreads / Amazon.

The canonical multi-seed run referenced below is:

```
python eval_harness.py --seeds 42,1,7 --llm-sample 400 --bertscore --persona-mode synth
```

| Paper location | Figures | Regenerated by |
|---|---|---|
| §4.2 RMSE table — V0/V1/V2, best α | 0.984/1.035/0.908 → 0.958/0.937/0.784 | canonical multi-seed run |
| §4.2 paired-seed V1−V2 test | +0.018 / +0.005 / +0.002 | `per_seed` block of `evaluation_results.json` (canonical run) |
| §4.2 two-populations variance buckets | 0.38/0.87/1.21 · 0.57/0.76/1.12 · 1.05/0.99/1.02 | `python analysis/study_data.py` |
| §4.2 V3 3-term calibration | V3 0.910 / 0.926 / 0.770 | `python analysis/measure_calib3.py` |
| §4.3 review quality — ROUGE-L, Semantic-BGE | 0.096/0.083/0.097 · 0.740/0.632/0.663 | canonical multi-seed run |
| §4.4 retrieval — dense/hybrid/CF/popularity | hybrid 0.184/0.040/0.051 | canonical multi-seed run |
| §4.4 hybrid dense/CF weight = 0.2 | leave-one-out HitRate@10 sweep | `python analysis/tune_hybrid.py` |
| §4.4 sampled-metric (101-candidate) NDCG@10 | 0.337 / 0.248 / 0.301 | `python eval_harness.py --no-llm --persona-mode synth --candidate-pool 101 --pop-distractors` |
| §4.5 cold-start — optimal α, RMSE curve | α 0.8/0.6/0.3 …; 1.421→1.227 etc. | `python eval_harness.py --llm-sample 400 --cold-start --bertscore --persona-mode synth` |
| §4.6 persona ablation — template vs synth | RMSE ≈ equal; Yelp HitRate@10 0.174 vs 0.198 | the canonical run with `--persona-mode template` vs `synth` (seed 42) |

## Discarded / superseded figures

- An earlier draft scored the LLM blend on an **n = 10** sample and reported a
  Goodreads V2 RMSE of **0.60**. The full-set multi-seed evaluation corrects
  this to **0.94** — the 0.60 was sampling noise and is **not used anywhere in
  the paper**. See `EXPERIMENTS.md` #1.

## Reproduction notes

- Results are written to `evaluation_results.json` and `evaluation_results.md`.
- The datasets (`data/*.csv`), synthesised personas (`data/synthesized_personas.json`)
  and item embeddings (`data/item_embeddings.npz`) are committed, so every
  command above runs fully offline.
- The complete per-run ledger — config, headline numbers, verdict — is
  `EXPERIMENTS.md`.
